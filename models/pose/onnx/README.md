# Pose ONNX Exports

Store yolov11 pose or RTMPose ONNX models here (e.g., `yolov11n-pose.onnx`). When the TensorRT engine is absent the pose detector falls back to these ONNX exports for CPU inference.
